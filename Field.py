class Field(object):

    def __init__(self):
        #Status hot 0, won 1, full 2 see docs
        self._state = 0

    def on_init(self):
        #Objekt Array
        cf = [[], [], []]
        for a in range(0, 3):
            for b in range(0, 3):
                f[a].append(C_Field())



    def bide(self):
        pass

